# Force npm to use exact versions
save-exact=true

# Disable package lock warnings
package-lock=true

# Rebuild native modules on install
rebuild-bundle=true

# Ensure sqlite3 is built for the correct platform
build-from-source=true
